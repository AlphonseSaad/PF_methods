[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "my_methods"
version = "0.1.3"
description = "PowerFactory helper functions"
authors = [{name="Alphonse Saad"}]
dependencies = []
